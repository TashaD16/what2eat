{
  "name": "what2eat-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev":      "turbo run dev",
    "build":    "turbo run build",
    "preview":  "turbo run preview",
    "lint":     "turbo run lint",
    "format":   "turbo run format",
    "test:e2e": "turbo run test:e2e"
  },
  "devDependencies": {
    "turbo":    "^2.3.0",
    "prettier": "^3.1.1"
  }
}
